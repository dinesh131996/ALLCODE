package com.java.scanner1;

import java.util.Scanner;


public class FindSymbolPresentOrNot {
	public static void main(String[] args) {
		
	Scanner m = new Scanner(System.in);
	System.out.print("Input:");
	String nextLine = m.nextLine();
	int g = nextLine.indexOf('@');
	if (g>=0) {
		System.out.println("valid");
		
	}
	else {
		System.out.println("invalid");
	}
	
	/*
	 * for (int i = 0; i <=nextLine.length()-1; i++) { switch (nextLine.charAt(i)) {
	 * case '@': System.out.println("valid email");
	 * System.out.println("@ is present"); break;
	 * 
	 * default:
	 * 
	 * 
	 * break; }
	 * 
	 * }
	 */
	
	
 
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		/*
		 * String s ="1"+"2"+"3"+"4";
		 * 
		 * int k = Integer.parseUnsignedInt(s); int i1 = Integer.parseInt(s);
		 * System.out.println(i1);
		 * 
		 * Scanner scanner = new Scanner(System.in); System.out.println("Input:");
		 * String next = scanner.next(); for
		 * 
		 * 
		 * int N = scanner.nextInt(); if (N<=20 && N>=2) {
		 * 
		 * 
		 * for(int i=1; i<=10;i++){ int k=0; k=N*i;
		 * System.out.println(N+" X "+i+" = "+k); }
		 * 
		 * scanner.close(); }
		 */
 
		}
}
//	int youtubeViews = 23333333;
	// Alwayz use camel notation in variables also
	//alwayz use full form of the letters bcoz its easly understandable

//	long views = 2333333333L;
	// if your given value is higher than the than the datatype
	// int views=2333333333;
//upto 2billion is stored in int so that we have to change to long
//previosly we enter assign the values to the int so compiler stores 
//the values so if we change to another data type we want to mention want type it is

